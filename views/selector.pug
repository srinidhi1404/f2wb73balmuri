extends layout

block content
  h1= title
  - var image_names=["hersheys1.jpg","hersheys2.webp","kitkat1.jpg","kitkat2.jpg","toblerone.webp"]
  - for(let i=0;i<3;i++){
     - let random= Math.floor(Math.random()*5)
     img(src=`/images/${image_names[random]}` alt=`Image No ${random}` height="300px" width="300px")
  - }
